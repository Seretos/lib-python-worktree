"""Stub tests for the full teardown module (W8).

The teardown module (process termination, contract teardown: steps, port
release) is not yet implemented. These placeholders will be filled in when
W8 lands.
"""

import pytest


@pytest.mark.skip(reason="teardown module (W8) not yet implemented")
def test_teardown_placeholder():
    pass
